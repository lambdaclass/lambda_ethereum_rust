[package]
name = "zkvm_interface"
version = "0.1.0"
edition = "2021"

[dependencies]
ethereum_rust-storage = { path = "../../../../storage/store" }

[build-dependencies]
risc0-build = { version = "1.1.2" }

[package.metadata.risc0]
methods = ["guest"]

[features]
default = []
build_zkvm = []
