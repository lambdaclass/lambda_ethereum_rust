apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prom-001
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: POST
    readOnly: false
    editable: true
